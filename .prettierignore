.DS_Store
node_modules

# Build output. These need to be depth-independent: a leading-slash pattern is
# root-relative, so `/.svelte-kit` did NOT cover apps/docs/.svelte-kit or
# packages/sve-ui/dist, and prettier was walking generated files.
**/build
**/.svelte-kit
**/dist
**/package
.vercel
.turbo

# Generated from the component sources by `pnpm gen:props` — formatting it would
# fight the generator and break the drift guard.
apps/docs/src/lib/docs/generated/

# Written by `pnpm check:render:update`, for the same reason: prettier and the
# script would rewrite each other on every baseline update.
apps/docs/render-baseline.json
.env
.env.*
!.env.example

# Ignore files for PNPM, NPM and YARN
pnpm-lock.yaml
package-lock.json
yarn.lock
