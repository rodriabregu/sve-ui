## SDD Configuration — sve-ui
## Generated by sdd-init on 2026-06-20
## Mode: hybrid (openspec + engram)

project: sve-ui
artifact_store: hybrid
branch: feat/2026-rebuild
initialized: 2026-06-20

context:
  type: pnpm + Turborepo monorepo
  package_manager: pnpm@11.8.0
  node: ">=22.14.0"
  packages:
    - packages/sve-ui       # published library (Svelte 5 rebuild)
    - packages/typescript-config
    - packages/eslint-config
    - apps/docs             # SvelteKit 2 + Tailwind 4
  product_vision: >
    HeroUI/NextUI-style accessible Svelte 5 component library.
    No Tailwind required in consumer projects. Theme via CSS custom properties.
    Foundation: Bits UI 2.18.1 for interactive components.

stack:
  framework: Svelte 5.56 / SvelteKit 2.66
  build: Vite 8 + @sveltejs/package 2.5.8 (NOT tsup)
  test_runner: Vitest 4.1.9
  component_testing: "@testing-library/svelte 5.3.1"
  lint_hybrid:
    - oxlint 1.70.0           # .ts/.js only
    - eslint 10 (flat config) # .svelte templates via eslint-plugin-svelte
  formatter: Prettier 3.8.4 + prettier-plugin-svelte
  type_checker: TypeScript 6.0.3 + svelte-check
  releases: Changesets + npm Trusted Publishing (OIDC)
  ui_foundation: bits-ui 2.18.1

testing:
  strict_tdd: true
  status: provisional           # node_modules not installed yet
  unit:
    available: true
    command: "pnpm test"        # turbo → vitest in packages/sve-ui
    framework: Vitest 4.1.9
  component:
    available: true
    tool: "@testing-library/svelte 5.3.1"
  a11y:
    available: true
    tool: "@axe-core/playwright (planned per ROADMAP Phase 2)"
  e2e:
    available: false
    tool: "—"
  coverage:
    available: true
    command: "pnpm test -- --coverage"
  quality:
    lint: "turbo run lint && oxlint"
    type_check: "turbo run check"
    format_check: "prettier --check ."

phases:
  proposal:
    required_inputs: []
    produces: proposal.md
  spec:
    required_inputs: [proposal.md]
    produces: spec.md
  design:
    required_inputs: [proposal.md]
    produces: design.md
  tasks:
    required_inputs: [spec.md, design.md]
    produces: tasks.md
  apply:
    required_inputs: [tasks.md, spec.md, design.md]
    produces: apply-progress.md
  verify:
    required_inputs: [spec.md, tasks.md, apply-progress.md]
    produces: verify-report.md
  archive:
    required_inputs: [all]
    produces: archive-report.md

pr_budget: 400
delivery_strategy: ask-on-risk
